apiVersion: 1

providers:
  - name: "OpenWrapper Dashboards"
    orgId: 1
    folder: "OpenWrapper"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
